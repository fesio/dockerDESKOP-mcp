services:
  evo-mcp:
    build:
      context: .
      target: host-tools
    environment:
      PROGRAMMER_ALLOW_DOCKER: "true"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    group_add:
      - "${DOCKER_GID:-0}"
